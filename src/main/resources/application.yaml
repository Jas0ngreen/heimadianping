server:
  port: 8081
spring:
  config:
    import: optional:classpath:application-local.yaml
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL:jdbc:mysql://127.0.0.1:3306/hmdp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:}
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: ${REDIS_DATABASE:1}
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
mybatis-plus:
  type-aliases-package: com.hmdp.entity # 别名扫描包
logging:
  level:
    com.hmdp: debug

langchain4j:
  open-ai:
    chat-model:
      base-url: https://open.bigmodel.cn/api/paas/v4
      api-key: ${ANTHROPIC_AUTH_TOKEN}
      model-name: glm-4
      log-requests: true
      log-responses: true

    embedding-model:
      base-url: https://open.bigmodel.cn/api/paas/v4
      api-key: ${ANTHROPIC_AUTH_TOKEN}
      model-name: embedding-3
      log-requests: true
      log-responses: true

#ai:
#  zhipu:
#    api-key: ${ZHIPU_API_KEY:}
#    chat-base-url: ${ZHIPU_CHAT_BASE_URL:https://open.bigmodel.cn/api/paas/v4/chat/completions}
#    embedding-base-url: ${ZHIPU_EMBEDDING_BASE_URL:https://open.bigmodel.cn/api/paas/v4/embeddings}
#    chat-model: ${ZHIPU_CHAT_MODEL:glm-4-flash}
#    embedding-model: ${ZHIPU_EMBEDDING_MODEL:embedding-3}
